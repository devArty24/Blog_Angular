// Define model and export this model
export class Pelicula{
    /* public title: string;
    public year: number;
    public image: string;
    
    // Define params arrived
    constructor(title, year, image){
        this.title = title;
        this.year = year;
        this.image = image;
    } */

    constructor(
        public title: string,
        public year: number,
        public image: string
    ){}
}